# 评分系统优化迭代流程

> 以 54 份基准提交为回归集的评分质量闭环：发现问题 → 归因 → 修改（examSystem / subjective-scoring / 试卷数据三层）→ 测试 → 重评 → 对比 → 发布 → 下一轮。
> 本文档记录 2026-08-01 两轮真实迭代（有界修正轮、代码题轮）沉淀下来的完整操作序列与踩坑记录。

## 0. 基准与基础设施

| 资产 | 位置 | 说明 |
|---|---|---|
| 54 份基准提交 | dev DB `submissions` id 3–56 | 18 套试卷 × 低/中/高 三档，走真实考试 API 产生 |
| 逐题盲评估计（真值近似） | `benchmarks/subjective-54/est_perq.json` | 按参考答案人工盲评，不看系统分 |
| 整卷盲评估计 | `benchmarks/subjective-54/est_totals.json` | |
| v0.1.7 固定基线 | `benchmarks/subjective-54/baseline_v017_*` | 首轮系统分存档，**永不更新** |
| 对比脚本 | `benchmarks/subjective-54/compare.py` | 实时读 DB 与基线/估计对比 |
| 评分包源码 | `/Users/yhw/Code/Github/subjective-scoring` | GitHub yhwyxy/subjective-scoring，push 走 SSH |

核心判定指标（`compare.py` 输出）：

- **逐题 MAE / 偏差 / 误差 ≤3 分题数**（177 道主观题）
- **degraded**：基线误差 ≤3 且当前 >3 —— *每一例都必须逐个归因，这是迭代的红线*
- **improved**：基线 >3 且当前 ≤3
- **still >3**：下一轮优化候选，按误差降序

## 1. 发现问题

三个信息源，按顺序看：

```bash
# ① 全量指标 + 退化/改善/仍超差清单
python3 benchmarks/subjective-54/compare.py
python3 benchmarks/subjective-54/compare.py --paper software-development   # 聚焦某卷

# ② 单题诊断：grading_detail_json 的 reason 是引擎决策的完整轨迹
#    （实体门槛/保底/AST 失败/冲突封顶 等都会写进 reason）
psql "$DATABASE_URL" -c "SELECT grading_detail_json FROM submissions WHERE id=33;"

# ③ worker 日志：模型是否真的在跑（毫秒级完成 = 词法回退 = 无效结果）
grep "加载 CrossEncoder 失败" /tmp/scoring_worker.log
tail /tmp/scoring_worker.log   # 正常应看到 Batches 推理进度条、秒级 job
```

## 2. 归因：问题属于哪一层

| 层 | 典型症状 | 真实案例 |
|---|---|---|
| **subjective-scoring 包** | 评分规则本身的系统性偏差 | 套话骗分（+5~+19）、简洁数值答案压分（−17）、"越大 vs 增大"bigram 断裂误伤 |
| **examSystem worker**（grader_bridge） | 字段没传到引擎，引擎行为"看起来像缺功能" | `code_language` 只认 type∈{code,sql} 被丢弃 → JS 代码按 python 解析 → AST 全灭 → 0 分 |
| **试卷数据 / 快照** | 引擎有能力但数据没启用；评分点本身质量差 | 代码题未配 `code_scoring_profile`；环保 q44 评分点是"答出参考答案之一"这类元描述 |

> 教训：先确认"能力是否存在、数据是否到达引擎"，再谈改算法。代码题 0 分曾被误判为"需要 LLM judge"，实际是接线 bug + 数据未配置。

## 3. 修改

### 3a. 改 subjective-scoring（评分算法）

```bash
cd /Users/yhw/Code/Github/subjective-scoring
git checkout -b feat/<topic>          # 基于 main
# ... 修改 src/，每个真实误伤/漏拦案例固化为回归测试写进 tests/ ...
uv run pytest -q                      # 全量测试（当前 180 个）
git add -A && git commit && git push -u origin feat/<topic>
```

设计约束（有界修正原则）：压分规则只在自己的触发区起作用（如实体零命中），抬分规则只升不降（max 保底），两类触发条件互斥，保证不扰动已打准的区间。

### 3b. 改 examSystem worker

```bash
# 改 scoring_worker/*.py 后：
.venv/bin/python -m pytest tests/worker -q
```

### 3c. 改试卷数据（data/papers/*.json）

**关键坑：重评读的是 run 快照，不是 data/papers。** 改题目字段必须三步：

```bash
# ① 改 data/papers/<slug>.json（对未来新开的 run 生效）
# ② 同步改快照文件（对既有 run 的重评生效）
#    data/exam_runs/paper-<slug>-run-<run_id>.json
# ③ 快照有 sha256 校验，改完必须更新 DB：
python3 -c "import hashlib;print(hashlib.sha256(open('<快照路径>','rb').read()).hexdigest())"
psql "$DATABASE_URL" -c "UPDATE exam_runs SET snapshot_hash='<新hash>' WHERE id='<run_id>';"
```

## 4. 安装新包 + 重启 worker

```bash
cd /Users/yhw/Code/Github/examSystem

# 装包需要代理（GitHub），注意 uv 缓存坑（见下）
uv pip install --no-cache --python .venv/bin/python --no-deps --force-reinstall \
  "subjective-scoring @ git+https://github.com/yhwyxy/subjective-scoring@<branch或tag>"

# 验证装的是新代码（不能只信安装日志里的 commit 号！）
.venv/bin/python -c "from subjective_scoring.engines.code_hybrid import TreeSitterAstExtractor; \
  assert hasattr(TreeSitterAstExtractor, '_extract_code_block'); print('fresh OK')"

# 重启 worker：跑模型必须【去掉代理】+ HF 离线（与装包的代理要求相反）
kill $(ps aux | grep "python -m scoring_worker" | grep -v grep | awk '{print $2}')
set -a && source .env && set +a
unset ALL_PROXY all_proxy HTTP_PROXY http_proxy HTTPS_PROXY https_proxy NO_PROXY no_proxy
export DATABASE_URL='postgres://exam_app:exam_app_dev@127.0.0.1:5432/exam_system?sslmode=disable' \
       HF_HUB_OFFLINE=1 TRANSFORMERS_OFFLINE=1
nohup .venv/bin/python -m scoring_worker >> /tmp/scoring_worker.log 2>&1 &
```

**两个必踩的坑：**

1. **uv 缓存**：同版本号的 git 依赖重装时 uv 复用缓存构建产物——安装日志显示新 commit，文件却是旧的。分支迭代期（版本号不变）必须 `uv cache clean subjective-scoring` + `--no-cache`，装完 grep 新符号确认。曾导致一轮"语言探测"看似无效。
2. **代理变量**：本机代理（socks5://127.0.0.1:6153）会让 huggingface_hub 的 httpx 因缺 socksio 直接 ImportError，且发生在建客户端阶段，`HF_HUB_OFFLINE` 都救不了；而 `resolve_pair_scorer` 会把加载失败**静默回退**为词法相似度——worker 不崩、30ms/份出无效分。判据：job 完成耗时毫秒级 = 回退了。

## 5. 重新评分

```bash
TOKEN=$(curl -s -X POST http://127.0.0.1:8000/api/admin/login \
  -H 'Content-Type: application/json' -d '{"password":"admin123"}' \
  | python3 -c "import sys,json;print(json.load(sys.stdin)['token'])")

# 全量 54 份（约 3–4 分钟）；聚焦单卷时只 regrade 对应 id
for id in $(seq 3 56); do
  curl -s -X POST "http://127.0.0.1:8000/api/admin/regrade/$id" \
    -H "Authorization: Bearer $TOKEN" > /dev/null
done

# 轮询本代 job 全部 done（generation 取当前最大值）
psql "$DATABASE_URL" -t -c \
  "SELECT status, count(*) FROM grading_jobs WHERE generation=(SELECT max(generation) FROM grading_jobs) GROUP BY status;"
```

## 6. 对比与判定

```bash
python3 benchmarks/subjective-54/compare.py
```

判定顺序：

1. **degraded 清单逐例归因**（红线）：取该题 `grading_detail_json` 的 reason，区分"新规则误伤"（必须修）/"其他改动所致"/"踩线波动"（如 3.3 vs 3.0）。
2. 核心指标不得倒退：MAE、偏差绝对值、≤3 题数。
3. **still >3 清单分类**，形成下一轮的优化候选（见 §8）。

## 7. 发布

```bash
cd /Users/yhw/Code/Github/subjective-scoring
git checkout main && git merge --ff-only feat/<topic>
# bump pyproject.toml 的 version，commit
git tag v0.1.X && git push origin main v0.1.X

cd /Users/yhw/Code/Github/examSystem
# pyproject.toml [tool.uv.sources] 的 tag 改为 v0.1.X
uv lock --upgrade-package subjective-scoring
uv pip install --no-cache --python .venv/bin/python --no-deps --force-reinstall \
  "subjective-scoring @ git+https://github.com/yhwyxy/subjective-scoring@v0.1.X"
# 重启 worker（§4），必要时抽查重评一份确认
```

发布后回到 §1 开始下一轮。

## 8. 迭代实录与当前状态

### 第一轮：有界修正（v0.1.7 → f7690c4）

- 问题：套话骗分（低档 +5~+19）、简洁数值答案压分（−17）、措辞差异稀释。
- 修改：实体门槛（整卷零命中 ×0.2）、数值保底（数字全对→sim 0.9）、短答案保底（覆盖率 ≥0.8→sim 0.7），`engines/entities.py` + `text_reranker.py`。
- 中途误伤即修：门槛第一版"逐点零命中"误伤合法改述 → 收紧为"整卷零命中"；"越大 vs 增大"bigram 断裂 → 删程度填充字；真值≈真实值 → 等价表；环保"XX污染"单实体点漏拦 → 整卷实体池 ≥3 也纳入门槛。
- 结果：逐题 MAE 2.47→1.74，偏差 −0.77→−0.39，≤3 题数 130→146。

### 第二轮：代码题（→ v0.1.8）

- 问题：software-development q16/q21 得 0~1.3 分（高档整卷 −24.9）。
- 归因：worker 丢 `code_language`（按 python 解析 JS → AST 全灭）+ 试卷未配 `code_scoring_profile` + 作答是"说明文字+代码"混排（题目要求标明语言，混排是必然）。
- 修改：grader_bridge 透传语言与 profile；包侧 `extract_auto` 语言探测 + 混排代码块剥离；数据/快照配 `nested_loop_static` / `find_index_static`。
- 结果：q21 0→15/13（估 15/12.5），q16 1.3→9（估 10/9），高档整卷误差 −24.9→+1.2。

### 当前累计（v0.1.8，2026-08-01）

逐题 MAE **2.47→1.50**，偏差 **−0.77→−0.14**，≤3 题数 **130→150/177**；整卷总分 MAE **6.71→3.81**、≤3 分的卷 18/54→28/54，三档平均分全面向盲评估计收拢且排序零倒挂。

## 9. 已知遗留优化点（下一轮候选）

| 案例 | 现状 vs 估计 | 归因 | 候选方案 |
|---|---|---|---|
| communications SIM-M q44 | 10 vs 0 | 覆盖度 0.85 满分规则放大了高相似错误答案 | 0.85 规则叠加实体命中前置条件 |
| welding SIM-L q41 | 13 vs 0 | 答案复述题干作业流程词汇，实体全命中，门槛无法区分 | 题干复述检测（答案与题干 n-gram 重叠率过高时降权） |
| environmental SIM-L q44 | 8.2 vs 0 | 评分点是"答出参考答案中的工作之一"元描述，无实体可抽 | 数据层重写评分点为具体条目（超低排放/清江行动…） |
| chemical-engineering SIM-M q43 | 1.3 vs 6 | 门槛对部分正确的中档答案误伤 | 已被 need_review 兜底；观察是否成模式再定 |
| legal q35 复合大题 | 3.3 vs 0 | 踩线波动（32 分大题） | 观察 |

> 人工兜底始终有效：54 份全部 need_review，误伤案例会进人工复核队列，这是所有自动规则的安全网。
