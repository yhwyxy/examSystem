# 企业在线考试兼批改系统 - 后端依赖
# 核心依赖（必装）
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pyyaml>=6.0
qrcode[pil]>=7.4
openpyxl>=3.1
httpx>=0.27
python-dotenv>=1.0
subjective-scoring[text,sql,code,remote] @ git+https://github.com/yhwyxy/subjective-scoring.git@v0.1.4

# 以下为本地主观题 CrossEncoder 依赖（可选）。
# 未安装时系统会自动降级到关键词重叠相似度，主流程仍可正常运行。
# 若首次安装失败（常见于 Python 3.14），可先注释掉这两行，待模型可用后再补装。
# sentence-transformers>=3.0
# torch>=2.2
