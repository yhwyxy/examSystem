# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Editor / IDE
.idea/
.vscode/
.DS_Store
*.swp

# Local sandbox / agent metadata
.aionrs/
.codex/
.worktrees/
.claude

# Local logs / runtime
*.log
*.db
*.sqlite
*.sqlite3

# Local environment secrets
.env
.env.*
!.env.example
!.env.production.example
# 生产配置 (含管理端密码等, 从 example 复制后填写, 不提交)
config.production.yaml

answer.js

# Exam run snapshots & public tokens (local runtime)
data/exam_runs/

# 本地 DB dump (含真实作答/员工 PII, 仅作备份, 不提交)
exam_system.sql

# Local model caches / vendored wheels
models/
packages/
benchmarks/
logs/

# 本地工作文档 (不提交)
docs/*
# 例外: 部署文档保持入库 (DEPLOY.md / README 引用, 需随代码演进)
!docs/deployment-*.md
评分系统迭代流程.md
主观题评分误差分析.md

# task6: 临时构建产物 (go build ./cmd/exam-server -> 当前 binary)
/exam-server

# task13: k6 容量测试产物
# 忽略: 运行时生成的 paper/sessions/原始 k6 JSON (含 token, 不可提交)
# 保留: scripts/loadtest/ (脚本本身) + loadtest/README.md + loadtest/results/summary.json
loadtest/frontend/
loadtest/scenarios/
loadtest/generated/
loadtest/results/*
!loadtest/results/summary.json
!loadtest/.gitkeep
.opencode/
